/**
 * @deprecated The hardcoded drum role/subRole taxonomy was removed in
 * Phase 0.8. Folder names under the drum library root ARE the roles now
 * (flat taxonomy). This file is kept as a tombstone for any old imports;
 * new code should not import from it.
 *
 * If you need to check whether a given role string is valid, ask the
 * kit-resolver instead — `kitResolver.getDiscoveredRoles()` returns the
 * live list discovered from disk.
 */

export {};
